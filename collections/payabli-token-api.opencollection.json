{
  "opencollection": "1.0.0",
  "info": {
    "name": "API reference Bill Token API",
    "version": "1.0.0"
  },
  "items": [
    {
      "info": {
        "name": "Token",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Get an access token (server-side)",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api-sandbox.payabli.com/api/v2/Token/serverside",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Exchanges a client ID and client secret for a short-lived Bearer access token using the OAuth2 client-credentials flow. Designed for server-to-server use: the credentials and the returned token stay on your backend. Send the returned `access_token` in the `Authorization` header as `Bearer <access_token>` on subsequent API calls. See the [OAuth authentication guide](/developers/oauth-authentication) for the full flow."
        }
      ]
    }
  ],
  "bundled": true
}