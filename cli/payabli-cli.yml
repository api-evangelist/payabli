generated: '2026-07-20'
method: searched
source: https://docs.payabli.com/developers/platform-developer-query-cli
cli:
  name: PayQuery CLI
  repo: https://github.com/payabli/payquery-cli
  docs: https://docs.payabli.com/developers/platform-developer-query-cli
  description: >-
    First-party CLI for running queries against Payabli data (transactions, settlements, batches,
    transfers, and other Query-tag resources) from the terminal.
  install:
  - method: github-release
    url: https://github.com/payabli/payquery-cli/releases
  surface:
  - group: query
    description: Query Payabli data resources (transactions, settlements, batches, transfers).
notes: >-
  The CLI binary is catalogued in packages/payabli-packages.yml. Command surface captured at the
  group level; see the docs guide for the full flag reference.
